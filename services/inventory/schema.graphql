type Product @extends @key(fields: "upc") {
    upc: String! @external @id
    weight: Int @external
    price: Int @external
    inStock: Boolean
    shippingEstimate: Int @requires(fields: "price weight")
}
