type Product @extends @key(fields: "upc") {
    upc: String! @external @id
    inStock: Boolean
    shippingEstimate: Int
}

# For Dgraph
type Query {
  shippingEstimate: Int @custom(dql: "{me(){}}")
}
